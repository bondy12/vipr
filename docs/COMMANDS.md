# Commands

```bash
vipr demo
vipr profile demo
vipr profile demo --json
vipr rules
vipr doctor
```

`demo` and `profile demo` are fully offline and deterministic. Live wallet lookup is deliberately not exposed until the attribution and provenance adapter exists.
